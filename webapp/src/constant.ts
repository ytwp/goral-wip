export enum StoreKey {
  Access = "access-control",
  Config = "app-config",
}

export enum Path {
  Base = "/",
  Dashboard = "/dashboard",
  Query = "/query",
  Setting = "/setting",
  SignIn = "/sign-in",
  SignUp = "/sign-up",
  ForgotPassword = "/forgot-password",
}

export enum Theme {
  System = "system",
  Dark = "dark",
  Light = "light",
}